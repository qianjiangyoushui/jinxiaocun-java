<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Watering">
	
	<!-- 缓存条数 -->
	<cache size="2000" />
	
	<!-- 条件语句 -->
	<sql id="WhereSQL">
		<where>
			<if test="guid != null and guid != ''"> 
			AND T.GUID = #{guid} 
			</if>
			<if test="seedfileid != null and seedfileid != ''"> 
			AND T.SEEDFILEID = #{seedfileid} 
			</if>
			<if test="irrigatedate != null and irrigatedate != ''"> 
			AND T.IRRIGATEDATE = #{irrigatedate} 
			</if>
			<if test="irrigatestyle != null and irrigatestyle != ''"> 
			AND T.IRRIGATESTYLE = #{irrigatestyle} 
			</if>
			<if test="irrigatewater != null and irrigatewater != ''"> 
			AND T.IRRIGATEWATER = #{irrigatewater} 
			</if>
			<if test="irrigatelength != null and irrigatelength != ''"> 
			AND T.IRRIGATELENGTH = #{irrigatelength} 
			</if>
			<if test="remark != null and remark != ''"> 
			AND T.REMARK = #{remark} 
			</if>
			<if test="recordperson != null and recordperson != ''"> 
			AND T.RECORDPERSON = #{recordperson} 
			</if>
			<if test="principal != null and principal != ''"> 
			AND T.PRINCIPAL = #{principal} 
			</if>
			<if test="createdate != null and createdate != ''"> 
			AND T.CREATEDATE = #{createdate} 
			</if>
			<if test="operatorid != null and operatorid != ''"> 
			AND T.OPERATORID = #{operatorid} 
			</if>
		</where>
	</sql>
	
	<!-- 查找语句 -->
	<select id="find" resultType="Watering" parameterType="Watering" flushCache="false" useCache="true">
    	SELECT 
	    	T.GUID AS guid, 
	    	T.SEEDFILEID AS seedfileid, 
	    	T.IRRIGATEDATE AS irrigatedate, 
	    	T.IRRIGATESTYLE AS irrigatestyle, 
	    	T.IRRIGATEWATER AS irrigatewater, 
	    	T.IRRIGATELENGTH AS irrigatelength, 
	    	T.REMARK AS remark, 
	    	T.RECORDPERSON AS recordperson, 
	    	T.PRINCIPAL AS principal, 
	    	T.CREATEDATE AS createdate, 
	    	T.OPERATORID AS operatorid 
    	FROM watering T
		<include refid="WhereSQL" />
		ORDER BY T.CREATEDATE desc
	</select>
	
	<!-- 获取总记录数语句 -->
	<select id="findCount" resultType="java.lang.Integer" parameterType="Watering" flushCache="false" useCache="true">
    	SELECT COUNT(*)  FROM watering T
		<include refid="WhereSQL" />
	</select>
	
	<!-- 插入语句 -->
	<insert id="insert" parameterType="Watering" flushCache="true"> 
		INSERT INTO watering
		(
			GUID, 
			SEEDFILEID, 
			IRRIGATEDATE, 
			IRRIGATESTYLE, 
			IRRIGATEWATER, 
			IRRIGATELENGTH, 
			REMARK, 
			RECORDPERSON, 
			PRINCIPAL, 
			CREATEDATE, 
			OPERATORID 
		 ) 
		VALUES
		(
			#{guid,jdbcType=VARCHAR}, 
			#{seedfileid,jdbcType=VARCHAR}, 
			#{irrigatedate,jdbcType=DATE}, 
			#{irrigatestyle,jdbcType=VARCHAR}, 
			#{irrigatewater,jdbcType=VARCHAR}, 
			#{irrigatelength,jdbcType=VARCHAR}, 
			#{remark,jdbcType=VARCHAR}, 
			#{recordperson,jdbcType=VARCHAR}, 
			#{principal,jdbcType=VARCHAR}, 
			#{createdate,jdbcType=DATE}, 
			#{operatorid,jdbcType=VARCHAR} 
		) 
	</insert>
	
	<!-- 更新语句 -->
	<update id="update"  parameterType="Watering" flushCache="true"> 
		update watering 
		<set> 
			<if test="guid != null">GUID=#{guid},</if>
			<if test="seedfileid != null">SEEDFILEID=#{seedfileid},</if>
			<if test="irrigatedate != null">IRRIGATEDATE=#{irrigatedate},</if>
			<if test="irrigatestyle != null">IRRIGATESTYLE=#{irrigatestyle},</if>
			<if test="irrigatewater != null">IRRIGATEWATER=#{irrigatewater},</if>
			<if test="irrigatelength != null">IRRIGATELENGTH=#{irrigatelength},</if>
			<if test="remark != null">REMARK=#{remark},</if>
			<if test="recordperson != null">RECORDPERSON=#{recordperson},</if>
			<if test="principal != null">PRINCIPAL=#{principal},</if>
			<if test="createdate != null">CREATEDATE=#{createdate},</if>
			<if test="operatorid != null">OPERATORID=#{operatorid},</if>
		</set>
		WHERE GUID = #{guid}
	</update> 
	
	<!-- 单选删除语句 -->
	<delete id="delete" parameterType="Watering" flushCache="true"> 
		DELETE FROM watering WHERE GUID = #{guid}
	</delete>
	
	<!-- 多选删除 -->
	<delete id="mulDel" parameterType="java.lang.String" flushCache="true">
		DELETE FROM watering WHERE GUID IN
		<foreach item="id" collection="array" open="(" separator="," close=")"> 
			#{id}
		</foreach> 
	</delete>
</mapper>
